[workspace]
members = ["anchor-test", "program-test", "runtime"]
resolver = "2"

[workspace.package]
license = "MIT"
version = "0.1.0"
edition = "2021"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
